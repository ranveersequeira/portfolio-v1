import ProjectsList from "@/components/Projects";

const Projects = () => {
    return (
        <div className="space-y-12">
            <ProjectsList />
        </div>
    );
};

export default Projects;
